GraduateValidatorTest
├── when not the automator role
│   └── it should revert
└── when the automator role
    ├── when the validator registration block is zero
    │   └── it should revert
    └── when the validator registration block is not zero
        ├── when the validator is already graduated
        │   └── it should revert
        └── when the validator is not yet graduated
            ├── when the graduation duration is not yet reached
            │   └── it should revert
            └── when the graduation duration is reached
                ├── it should set validatorIsGraduated to true
                ├── it should distribute the pybapxETH shares
                └── it should emit ValidatorGraduated