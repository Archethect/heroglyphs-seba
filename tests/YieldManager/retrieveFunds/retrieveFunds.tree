RetrieveFundsTest
├── when the deposit amount is 0
│   └── it should revert
└── when the deposit amount is not zero
    ├── when the depositor is not the sender
    │   └── it should revert
    └── when the depositor is the sender
        ├── when the unlock time has not been reached
        │   └── it should revert
        └── when the unlock time has been reached
            ├── when the transfer of the funds to the receiver fails
            │   └── it should revert
            └── when the transfer of the funds to teh receiver succeeds
                ├── it should delete the deposits object
                ├── it should sent the funds back to the receiver
                └── it should emit FundsRetrieved