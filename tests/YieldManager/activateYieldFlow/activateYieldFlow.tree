ActivateYieldFlowTest
├── when not the automator role
│   └── it should revert
└── when the automator role
    ├── when the yield flow is already active
    │   └── it should revert
    └── when the yield flow is not yet active
        ├── it should set yieldFlowActive to true
        └── it should emit YieldFlowActivated