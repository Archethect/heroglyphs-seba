SwapExactEthForBoldTest
├── when the caller is not the yield manager
│   └── it should revert
└── when the caller is the yield manager
    ├── when the value is zero
    │   └── it should revert
    └── when the value is not zero
        ├── when the slippage is bigger or equal to the bps denominator
        │   └── it should revert
        └── when the slippage is smaller than the bps denominator
            ├── when there is already an open order
            │   └── it should revert
            └── when there is no open order yet
                ├── it should call createOrder with the correct order data and value
                ├── it should register the pending order in the contract
                └── it should emit IntentCreated