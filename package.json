{
  "name": "@prb/foundry-template",
  "description": "Foundry-based template for developing Solidity smart contracts",
  "version": "1.0.0",
  "author": {
    "name": "Paul Razvan Berg",
    "url": "https://github.com/PaulRBerg"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "@prb/test": "^0.6.4",
    "prettier-plugin-solidity": "^1.4.2",
    "solmate": "^6.8.0"
  },
  "devDependencies": {
    "forge-std": "github:foundry-rs/forge-std#v1.8.1",
    "prettier": "^3.0.0",
    "solhint": "^3.6.2"
  },
  "keywords": [
    "blockchain",
    "ethereum",
    "forge",
    "foundry",
    "smart-contracts",
    "solidity",
    "template"
  ],
  "private": true,
  "scripts": {
    "clean": "rm -rf cache out",
    "build": "forge build",
    "lint": "bun run lint:sol",
    "lint:sol": "prettier --check \"**/*.{sol,json,md,yml}\" --ignore-path \".prettierignore\" --plugin=prettier-plugin-solidity && bun solhint \"{script,src/{interfaces|lib|mocks|services},tests}/**/*.sol\"",
    "prettier:check": "prettier --check \"**/*.{sol,json,md,yml}\" --ignore-path \".prettierignore\" --plugin=prettier-plugin-solidity",
    "prettier:write": "prettier --write \"**/*.{sol,json,md,yml}\" --ignore-path \".prettierignore\" --plugin=prettier-plugin-solidity",
    "deploy:hoodi": "bash -c 'source .env && forge script Deploy --rpc-url $HOODI_RPC --account $HOODI_DEPLOYER_NAME --sender $HOODI_SENDER --broadcast --verify --resume --chain 560048 -vvvvv'",
    "deploy:holesky": "bash -c 'source .env && forge script Deploy --rpc-url $HOLESKY_RPC --account $HOLESKY_DEPLOYER_NAME --sender $HOLESKY_SENDER --verify --resume --chain holesky -vvvvv'",
    "deploy:mainnet": "bash -c 'source .env && forge script Deploy --rpc-url $MAINNET_RPC --account $MAINNET_DEPLOYER_NAME --sender $MAINNET_SENDER --verify --chain mainnet -vvvvv --resume'",
    "deploy:mainnet_test": "bash -c 'source .env && forge script DeployEthereumTest --rpc-url $MAINNET_RPC --account $HOODI_DEPLOYER_NAME --sender $HOODI_SENDER --verify --chain mainnet -vvvvv --broadcast --resume'",
    "test": "forge test",
    "test:coverage": "forge coverage",
    "test:coverage:report": "forge coverage --report lcov && genhtml lcov.info --branch-coverage --output-dir coverage"
  }
}
